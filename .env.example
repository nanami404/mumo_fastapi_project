# 项目配置
PROJECT_NAME=Mumo FastAPI Project
API_V1_STR=/api/v1

# 数据库配置
DATABASE_URL=sqlite:///./mumo_fastapi.db
# 对于PostgreSQL，使用类似：postgresql://user:password@localhost/dbname
# 对于MySQL，使用类似：mysql://user:password@localhost/dbname

# 安全配置
SECRET_KEY=your-super-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS配置（多个域名用逗号分隔）
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# 日志配置
LOG_LEVEL=INFO